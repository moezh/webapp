{
  "name": "webapp",
  "private": true,
  "workspaces": [
    "workspaces/*"
  ],
  "scripts": {
    "stop": "docker compose down",
    "dev": "bun run stop && bun install && bun create-env.ts development && docker compose up --build",
    "prod": "bun run stop && bun install && bun create-env.ts production && docker compose up --build -d",
    "clean": "rm -rf node_modules && bun --filter='*' clean"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
