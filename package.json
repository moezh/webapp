{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "env:dev": "bun create-env.ts development",
    "env:prod": "bun create-env.ts production",
    "astro": "astro",
    "app:dev": "astro dev",
    "app:prod": "rm -rf dist && astro build && bun ./dist/server/entry.mjs",
    "stop": "docker compose down && rm -rf .astro dist node_modules",
    "dev": "bun run stop && bun run env:dev && bun install && docker compose up --build",
    "prod": "bun run stop && bun run env:prod && bun install && docker compose up --build -d"
  },
  "dependencies": {
    "@astrojs/node": "latest",
    "@tailwindcss/vite": "latest",
    "astro": "latest",
    "tailwindcss": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
